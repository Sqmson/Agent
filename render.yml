services:
  - type: web
    name: Agent
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: API_KEY
        sync: false
